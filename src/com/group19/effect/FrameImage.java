package com.group19.effect;

/*
import java.awt.Graphics;
import java.awt.Graphics2D; // ve co ban thi 2D hay ko thi cx giong nhau nma de 2D cu the hon con cai khong co 2D thi nhieu ham nhan lam tham so
import java.awt.image.BufferedImage;


public class FrameImage {
     private String name;
	private BufferedImage image; // dung de luu tru hinh anh

	public FrameImage(String name1, BufferedImage image1) {
		this.name = name1;
		this.image = image1;
	}
	
	public FrameImage(FrameImage frameImage) {
		
		image = new BufferedImage(frameImage.getImageWidth(), frameImage.getImageHeight(),frameImage.getImage().getType());
		Graphics g = image.getGraphics(); // graphics nhu kieu cọ vẽ của image
		g.drawImage(frameImage.getImage(),0,0,null);
		
	}
	
	FrameImage(){
		
		image = null;
		name = null;
		
	}
	
	public void draw(Graphics2D g2, int x, int y) {
		g2.drawImage(image,x - image.getWidth()/2, y - image.getHeight()/2,null);
	}
	
	public int getImageWidth() {
		return image.getWidth();
	}
	
	public int getImageHeight() {
		return image.getHeight();
	}
	
	public String getName() {
		return name;
	}
	
	public void setName(String name1) {
		name = name1;
	}
	
	public BufferedImage getImage() {
		return image;
	}
	
	public void setImage(BufferedImage image1) {
		image = image1;
	}
 */

}
